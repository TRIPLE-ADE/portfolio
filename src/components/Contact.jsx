import { section } from "../style"
const Contact = () => (
    <section id="contact">
      <h2 className={`${section.heading} ${section.MarginBottom}`}> Contact </h2>
    </section>
  )

export default Contact